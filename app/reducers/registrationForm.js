const initialState = {
  name: '',
  surname: '',
  email: '',
  password: ''
};

export default function auth(state = initialState, action) {
  switch (action.type) {
    default :
      return state
  }
}