const initialState = {
  id: '',
  date: '',
  distance: '',
  time: '',

  status: '',
  error: '',
};

export default (state = initialState, action) => {
  switch (action.type) {
    default :
      return state
  }
}